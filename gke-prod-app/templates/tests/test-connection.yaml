apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gke-prod-app.fullname" . }}-test-connection"
  labels:
    {{- include "gke-prod-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gke-prod-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
